# ==============================================================================
# תצורת פריסה (Deployment) - תיקון משתני סביבה לחיבור דאטה-בייס
# ==============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: status-page
spec:
  replicas: 1
  selector:
    matchLabels:
      app: statuspage
  template:
    metadata:
      labels:
        app: statuspage
    spec:
      initContainers:
        - name: run-migrations
          image: 992382545251.dkr.ecr.us-east-1.amazonaws.com/yarin-noa-project:latest
          imagePullPolicy: Always
          workingDir: /app/statuspage
          command: ["/bin/sh", "-c"]
          args:
            - |
              # הרצת המיגרציות - הפקודה תמצא את manage.py בתיקייה הנוכחית
              python manage.py migrate --noinput
          env:
            - name: DB_HOST
              value: "statuspage-postgres-postgresql.default.svc.cluster.local"
            - name: DB_NAME
              value: "statuspage"
            - name: DB_USER
              value: "postgres"
            - name: REDIS_HOST
              value: "statuspage-redis-master.default.svc.cluster.local"
            # הוספת POSTGRES_PASSWORD ל-Init Container (תיקון השגיאה fe_sendauth)
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: statuspage-app-secrets
                  key: DB_PASSWORD
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: statuspage-app-secrets
                  key: DB_PASSWORD
          envFrom:
            - secretRef:
                name: statuspage-app-secrets

      containers:
      - name: statuspage-container
        image: 992382545251.dkr.ecr.us-east-1.amazonaws.com/yarin-noa-project:latest
        imagePullPolicy: Always
        workingDir: /app/statuspage
        ports:
          - containerPort: 8000
        command: ["/bin/sh", "-c"]
        args:
          - |
            # הרצת השרת - gunicorn יחפש את התיקייה statuspage בתוך התיקייה הנוכחית
            gunicorn --bind 0.0.0.0:8000 statuspage.wsgi:application
        env:
          - name: DB_HOST
            value: "statuspage-postgres-postgresql.default.svc.cluster.local"
          - name: DB_NAME
            value: "statuspage"
          - name: DB_USER
            value: "postgres"
          - name: REDIS_HOST
            value: "statuspage-redis-master.default.svc.cluster.local"
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: statuspage-app-secrets
                key: DB_PASSWORD
          - name: POSTGRES_PASSWORD
            valueFrom:
              secretKeyRef:
                name: statuspage-app-secrets
                key: DB_PASSWORD
        envFrom:
          - secretRef:
              name: statuspage-app-secrets